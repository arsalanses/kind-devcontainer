{
    "name": "kubernetes-tutorial-basic",

    "build": {
      "dockerfile": "Dockerfile",
      "args": { "KIND_VERSION": "0.19.0" }
    },

    "mounts": ["type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"],
  
    "settings": {
      "terminal.integrated.shell.linux": "/bin/bash"
    },

    "remoteUser": "root"
}
